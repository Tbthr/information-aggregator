---
phase: 03-topic-configuration-ai-optimization
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - prisma/schema.prisma
  - scripts/migrate-keyword-blacklist.ts
  - scripts/seed-topics.ts
  - src/reports/daily.ts
  - app/api/settings/reports/route.ts
  - components/report-settings-page.tsx
autonomous: false
requirements:
  - PIPELINE-02

must_haves:
  truths:
    - "Daily report filters content using Topic.excludeRules (not keywordBlacklist)"
    - "Preset topics exist in DB: AI Agent, LLM, 地缘冲突, 科技趋势, 商业财经, AI产品"
    - "DailyReportConfig.topicIds is initialized with all active topic IDs"
    - "keywordBlacklist field no longer exists in schema, API, or frontend"
  artifacts:
    - path: "scripts/migrate-keyword-blacklist.ts"
      provides: "One-time migration: keywordBlacklist -> Topic.excludeRules"
    - path: "scripts/seed-topics.ts"
      provides: "Topic seeding script with preset topics"
    - path: "prisma/schema.prisma"
      provides: "DailyReportConfig.keywordBlacklist removed"
      contains: "model DailyReportConfig"
      no_longer_contains: "keywordBlacklist"
    - path: "src/reports/daily.ts"
      provides: "filterContent uses Topic.excludeRules"
      contains: "Topic.excludeRules"
      no_longer_contains: "keywordBlacklist"
    - path: "app/api/settings/reports/route.ts"
      provides: "keywordBlacklist removed from API schema"
      no_longer_contains: "keywordBlacklist"
    - path: "components/report-settings-page.tsx"
      provides: "keywordBlacklist removed from frontend"
      no_longer_contains: "keywordBlacklist"
  key_links:
    - from: "scripts/migrate-keyword-blacklist.ts"
      to: "prisma.topic"
      via: "prisma.topic.update many"
      pattern: "prisma\\.topic\\.update"
    - from: "src/reports/daily.ts"
      to: "prisma.topic"
      via: "loadTopicsByIds + excludeRules matching"
      pattern: "excludeRules"
---

<objective>
Execute the keywordBlacklist migration to Topic.excludeRules, seed preset topics, and update filterContent in daily.ts. Remove keywordBlacklist from schema, API, and frontend.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@prisma/schema.prisma
@src/reports/daily.ts
@app/api/settings/reports/route.ts
@components/report-settings-page.tsx
@src/config/load-pack-prisma.ts

## Schema Facts (DailyReportConfig)
- keywordBlacklist: String[]  (to be removed)
- topicIds: String[]         (already exists)

## Schema Facts (Topic)
- excludeRules: String[] @default([])  (already exists, ready to use)

## filterContent current logic (daily.ts lines 198-225)
- Reads config.keywordBlacklist
- matchesBlacklist() checks title + body lowercase
- Topic filtering: content.topicIds matches config.topicIds
</context>

<tasks>

<task type="auto">
  <name>Task 1: Prisma schema - remove keywordBlacklist from DailyReportConfig</name>
  <files>prisma/schema.prisma</files>
  <action>
    Remove the `keywordBlacklist String[]` field from the `DailyReportConfig` model in prisma/schema.prisma.

    Before editing, read: prisma/schema.prisma

    Find the DailyReportConfig model block and remove this line:
    ```
    keywordBlacklist   String[]
    ```
    The field is on line 76. Remove only that line.
  </action>
  <verify>
    <automated>grep -n "keywordBlacklist" prisma/schema.prisma | wc -l</automated>
  </verify>
  <done>DailyReportConfig model no longer has keywordBlacklist field</done>
</task>

<task type="auto">
  <name>Task 2: Create one-time migration script - keywordBlacklist to Topic.excludeRules</name>
  <files>scripts/migrate-keyword-blacklist.ts</files>
  <action>
    Create a TypeScript migration script at `scripts/migrate-keyword-blacklist.ts` that:

    1. Reads the current `DailyReportConfig.keywordBlacklist` array
    2. For each keyword in the blacklist:
       - Creates a new Topic with `name="黑名单:{keyword}"` and `excludeRules=[keyword]` if it doesn't exist
       - OR appends the keyword to an existing Topic's excludeRules if already present
    3. Clears the keywordBlacklist field on DailyReportConfig (set to empty array)
    4. Logs the migration results

    Use `prisma.topic.upsert` to handle both create and update cases.

    The script must:
    - Import from `@/lib/prisma`
    - Use `prisma.dailyReportConfig.findUnique({ where: { id: "default" } })` to get current blacklist
    - Use `prisma.topic.upsert` for each keyword
    - Use `prisma.dailyReportConfig.update` to clear the blacklist
    - Log summary: how many topics created/updated, keywords migrated
    - Be runnable with: `npx tsx scripts/migrate-keyword-blacklist.ts`
  </action>
  <verify>
    <automated>ls scripts/migrate-keyword-blacklist.ts</automated>
  </verify>
  <done>Migration script exists and is runnable</done>
</task>

<task type="auto">
  <name>Task 3: Create topic seeding script with preset topics</name>
  <files>scripts/seed-topics.ts</files>
  <action>
    Create a TypeScript seeding script at `scripts/seed-topics.ts`.

    **Preset Topics to create (6 total):**

    | name | description | excludeRules (initial) |
    |------|-------------|----------------------|
    | AI Agent | AI Agent 产品、工具和平台动态 | [] |
    | LLM | 大语言模型（LLM）技术、论文和评测 | [] |
    | 地缘冲突 | 国际关系、地缘政治和冲突事件 | [] |
    | 科技趋势 | 新兴技术、科技行业趋势和商业模式 | [] |
    | 商业财经 | 商业、财经、投资和并购新闻 | [] |
    | AI产品 | AI 产品发布、用户体验和产品更新 | [] |

    The script must:
    - Import from `@/lib/prisma`
    - Use `prisma.topic.upsert` for each preset topic
    - Set `enabled: true`, `displayOrder` from 0-5
    - Set `excludeRules: []` initially (per D-05)
    - Set `includeRules: []` initially
    - Set `scoreBoost: 1.0`, `maxItems: 10`
    - Log which topics were created vs already existed
    - Initialize `DailyReportConfig.topicIds` with all 6 topic IDs (per D-06)

    Run with: `npx tsx scripts/seed-topics.ts`
  </action>
  <verify>
    <automated>ls scripts/seed-topics.ts</automated>
  </verify>
  <done>Topic seeding script exists and is runnable</done>
</task>

<task type="auto">
  <name>Task 4: Refactor filterContent in daily.ts to use Topic.excludeRules</name>
  <files>src/reports/daily.ts</files>
  <action>
    Refactor the `filterContent` function in `src/reports/daily.ts` to replace keywordBlacklist filtering with Topic.excludeRules matching.

    **Changes to make:**

    1. Remove the `keywordBlacklist` destructuring from `config` (line 202)
    2. Load topics with excludeRules: call `loadTopicsByIds(config.topicIds)` or `loadAllTopicsFromDb()` to get Topic records
    3. Replace `matchesBlacklist()` function logic:
       - OLD: checks if content title+body contains any keyword from config.keywordBlacklist
       - NEW: checks if content.topicIds contains a Topic that has any excludeRule matching content title+body
       - For each Topic whose ID is in content.topicIds AND whose excludeRules is non-empty:
         - If any excludeRule keyword appears in content title+body, filter OUT that content
    4. Remove all references to `keywordBlacklist` in the function

    **New excludeRules matching logic:**
    ```typescript
    // Build excludeRules map: topicId -> keywords[]
    const excludeRulesMap = new Map<string, string[]>()
    for (const topic of topics) {
      if (topic.excludeRules.length > 0) {
        excludeRulesMap.set(topic.id, topic.excludeRules)
      }
    }

    // Filter: exclude content if any of its topic's excludeRules match
    const filteredContents = topicFilteredContents.filter((content) => {
      for (const tid of content.topicIds) {
        const keywords = excludeRulesMap.get(tid)
        if (!keywords) continue
        const text = (content.title ?? "") + " " + (content.body ?? "")
        if (keywords.some((kw) => text.toLowerCase().includes(kw.toLowerCase()))) {
          return false  // excluded
        }
      }
      return true
    })
    ```

    Read first: src/reports/daily.ts (focus on filterContent function, lines 198-225)
    Also read: src/config/load-pack-prisma.ts (loadTopicsByIds, loadAllTopicsFromDb)
  </action>
  <verify>
    <automated>grep -n "keywordBlacklist" src/reports/daily.ts | wc -l && grep -n "excludeRules" src/reports/daily.ts | wc -l</automated>
  </verify>
  <done>filterContent uses Topic.excludeRules for content filtering, no keywordBlacklist references remain</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Tasks 1-4: Schema migration, keywordBlacklist removal, topic seeding, filterContent refactor</what-built>
  <how-to-verify>
    Run the following in order:

    1. **Schema update check:**
       ```
       grep -n "keywordBlacklist" prisma/schema.prisma
       ```
       Expected: no output (field removed)

    2. **Run migration script:**
       ```
       npx tsx scripts/migrate-keyword-blacklist.ts
       ```
       Expected: logs showing keywords migrated to Topic.excludeRules

    3. **Run topic seeding:**
       ```
       npx tsx scripts/seed-topics.ts
       ```
       Expected: logs showing 6 topics created/verified, DailyReportConfig.topicIds initialized

    4. **Verify daily.ts has no keywordBlacklist:**
       ```
       grep -n "keywordBlacklist" src/reports/daily.ts
       ```
       Expected: no output

    5. **Verify daily.ts uses excludeRules:**
       ```
       grep -n "excludeRules" src/reports/daily.ts
       ```
       Expected: output showing excludeRules is used

    6. **Push schema to database:**
       ```
       npx prisma db push
       ```
       Expected: schema updated successfully
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

<task type="auto">
  <name>Task 5: Remove keywordBlacklist from API schema and PUT handler</name>
  <files>app/api/settings/reports/route.ts</files>
  <action>
    In `app/api/settings/reports/route.ts`:

    1. Remove `keywordBlacklist` from `dailyConfigSchema` z.object (line 13):
       Remove: `keywordBlacklist: z.array(z.string()).optional(),`

    2. Remove `keywordBlacklist` from the PUT handler `updateData` construction (line 99):
       Remove: `if (dailyUpdate.keywordBlacklist !== undefined) updateData.keywordBlacklist = dailyUpdate.keywordBlacklist`
       Remove from upsert create: `keywordBlacklist: dailyUpdate.keywordBlacklist ?? existing?.keywordBlacklist ?? [],`

    3. Remove `keywordBlacklist` from GET response (line 67):
       It is spread via `...daily` so ensure it's not explicitly included.

    Read first: app/api/settings/reports/route.ts
  </action>
  <verify>
    <automated>grep -n "keywordBlacklist" app/api/settings/reports/route.ts | wc -l</automated>
  </verify>
  <done>keywordBlacklist removed from API schema and handlers</done>
</task>

<task type="auto">
  <name>Task 6: Remove keywordBlacklist from frontend ReportSettingsPage</name>
  <files>components/report-settings-page.tsx</files>
  <action>
    In `components/report-settings-page.tsx`:

    1. Remove `keywordBlacklist: string[]` from `DailyConfig` interface (line 22)

    2. Remove `keywordBlacklist: []` from the initial `daily` state (line 200)

    3. Remove the `keywordBlacklist` sync from `useEffect` (lines 225): remove `keywordBlacklist: (d.keywordBlacklist as string[]) ?? prev.keywordBlacklist`

    4. Remove from `handleSave` payload (line 264): remove `keywordBlacklist: daily.keywordBlacklist`

    5. Remove the entire "关键词黑名单" section from the JSX (around lines 379-391):
       ```jsx
       {/* 关键词黑名单 */}
       <div className="space-y-1.5">
         <Label className="text-sm font-medium">关键词黑名单</Label>
         ...
       </div>
       ```

    Read first: components/report-settings-page.tsx
  </action>
  <verify>
    <automated>grep -n "keywordBlacklist" components/report-settings-page.tsx | wc -l</automated>
  </verify>
  <done>keywordBlacklist removed from frontend DailyConfig interface and all JSX</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Tasks 5-6: API and frontend cleanup for keywordBlacklist removal</what-built>
  <how-to-verify>
    1. **API cleanup:**
       ```
       grep -n "keywordBlacklist" app/api/settings/reports/route.ts
       ```
       Expected: no output

    2. **Frontend cleanup:**
       ```
       grep -n "keywordBlacklist" components/report-settings-page.tsx
       ```
       Expected: no output

    3. **Build check:**
       ```
       pnpm build
       ```
       Expected: build succeeds
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<verification>
- grep "keywordBlacklist" returns 0 results in: prisma/schema.prisma, src/reports/daily.ts, app/api/settings/reports/route.ts, components/report-settings-page.tsx
- grep "excludeRules" returns results in src/reports/daily.ts
- Both scripts exist and are runnable: scripts/migrate-keyword-blacklist.ts, scripts/seed-topics.ts
- pnpm check passes
- pnpm build succeeds
</verification>

<success_criteria>
1. DailyReportConfig.keywordBlacklist field removed from schema
2. One-time migration script converts existing blacklist entries to Topic.excludeRules
3. 6 preset topics seeded: AI Agent, LLM, 地缘冲突, 科技趋势, 商业财经, AI产品
4. DailyReportConfig.topicIds initialized with all 6 topic IDs
5. daily.ts filterContent uses Topic.excludeRules (not keywordBlacklist)
6. keywordBlacklist removed from API schema, PUT handler, and GET response
7. keywordBlacklist removed from frontend ReportSettingsPage component
</success_criteria>

<output>
After completion, create `.planning/phases/03-topic-configuration-ai-optimization/03-01-SUMMARY.md`
</output>
