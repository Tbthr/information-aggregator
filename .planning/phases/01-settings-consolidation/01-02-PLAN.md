---
phase: 01-settings-consolidation
plan: "02"
type: execute
wave: 1
depends_on: []
files_modified:
  - app/settings/page.tsx
  - app/config/page.tsx
  - app/settings/reports/page.tsx
  - components/sidebar/sidebar.tsx
autonomous: true
requirements:
  - FRONTEND-02
  - FRONTEND-01

must_haves:
  truths:
    - "User visits /settings and sees 3 tabs: 日报, 周报, 数据源"
    - "User can switch between tabs and content changes accordingly"
    - "User visiting /config is redirected (301) to /settings?tab=sources"
    - "User visiting /settings/reports is redirected (301) to /settings"
    - "Sidebar '报告设置' nav links to /settings (Daily tab active)"
    - "Sidebar 'Config' nav links to /settings?tab=sources"
    - "Sources tab renders ConfigPage component"
    - "Daily/Weekly tabs render report settings form sections"
  artifacts:
    - path: app/settings/page.tsx
      provides: Unified tabbed settings page
      contains: "Tabs"
    - path: app/config/page.tsx
      provides: Redirect to /settings?tab=sources
      contains: "redirect"
    - path: app/settings/reports/page.tsx
      provides: Redirect to /settings
      contains: "redirect"
  key_links:
    - from: app/settings/page.tsx
      to: components/config-page.tsx
      via: import for Sources tab content
      pattern: "import.*ConfigPage"
    - from: app/settings/page.tsx
      to: components/report-settings-page.tsx
      via: import for Daily/Weekly tab content
      pattern: "import.*ReportSettingsPage"
    - from: app/settings/page.tsx
      to: components/ui/tabs.tsx
      via: Tabs, TabsList, TabsTrigger, TabsContent
      pattern: "from.*components/ui/tabs"

---

<objective>
Create the unified tabbed `/settings` page with Daily / Weekly / Sources tabs, and update routing for deprecated URLs and sidebar navigation. This plan also wires the corrected topicIds field from Plan 01 into the tabbed interface.
</objective>

<context>
@components/ui/tabs.tsx
@components/report-settings-page.tsx
@components/config-page.tsx
@components/sidebar/sidebar.tsx
@.planning/phases/01-settings-consolidation/01-01-PLAN.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create app/settings/page.tsx with 3-tab layout</name>
  <files>app/settings/page.tsx</files>
  <read_first>
    - components/ui/tabs.tsx (existing Radix tabs component)
    - components/report-settings-page.tsx (current implementation, updated with topicIds from Plan 01)
    - components/config-page.tsx (existing Sources config, will be imported)
  </read_first>
  <action>
    Create app/settings/page.tsx as a Next.js App Router page component.

    The page must:
    1. Use URL search params (?tab=daily|weekly|sources) for tab state persistence
    2. Use useSearchParams from next/navigation to read initial tab
    3. Use useRouter from next/navigation to update URL on tab change (router.replace)
    4. Import and use Tabs, TabsList, TabsTrigger, TabsContent from @/components/ui/tabs
    5. Render three tabs:
       - "日报" (Daily) tab content: use the daily-relevant section of ReportSettingsPage
       - "周报" (Weekly) tab content: use the weekly-relevant section of ReportSettingsPage
       - "数据源" (Sources) tab content: import and render ConfigPage from @/components/config-page
    6. For the ConfigPage import: ConfigPage has its own header strip. The tab bar in app/settings/page.tsx serves as the page-level navigation header. If ConfigPage renders double-header when embedded, accept the visual distinction or pass a hideHeader prop if that prop exists in ConfigPage.
    7. Use AppLayout wrapper (same as other pages, check existing page patterns)
    8. Page heading: "设置" (text-xl font-semibold)
    9. Tab bar styling: use the standard TabsList pattern from components/ui/tabs.tsx

    Note: ReportSettingsPage currently renders both daily and weekly forms in one component. For the tabbed layout, either:
    - Pass a prop to ReportSettingsPage to render only the daily or weekly section (if such prop exists), OR
    - Create wrapper components that isolate the daily and weekly sections
    - If ReportSettingsPage cannot be split easily, render it once per tab with internal tab state, but this is sub-optimal

    The Daily tab heading should be "日报配置" and Weekly tab heading should be "周报配置" per UI-SPEC copywriting contract.
  </action>
  <verify>
    <automated>
      # Verify settings page exists and contains Tabs
      grep -l "Tabs" app/settings/page.tsx && echo "File exists with Tabs - PASS"
      # Verify imports
      grep "from.*components/ui/tabs" app/settings/page.tsx
      grep "from.*components/config-page" app/settings/page.tsx
    </automated>
  </verify>
  <done>
    /settings page renders with 3 tabs, tab state persisted in URL, ConfigPage renders in Sources tab.
  </done>
</task>

<task type="auto">
  <name>Task 2: Create redirect routes for deprecated URLs</name>
  <files>app/config/page.tsx, app/settings/reports/page.tsx</files>
  <read_first>
    - app/config/page.tsx (existing file)
    - app/settings/reports/page.tsx (existing file, if it exists)
  </read_first>
  <action>
    File: app/config/page.tsx
    Replace the entire content with:
    ```typescript
    import { redirect } from "next/navigation"
    export default function ConfigRoute() {
      redirect("/settings?tab=sources")
    }
    ```

    File: app/settings/reports/page.tsx
    If this file already exists, replace the entire content with:
    ```typescript
    import { redirect } from "next/navigation"
    export default function SettingsReportsRoute() {
      redirect("/settings")
    }
    ```
    If the file does not exist, create it with the redirect content above.

    These are permanent 308 redirects. Browsers will treat them as 301.
  </action>
  <verify>
    <automated>
      grep "redirect" app/config/page.tsx | grep "settings?tab=sources"
      grep "redirect" app/settings/reports/page.tsx 2>/dev/null | grep "settings" || echo "redirect exists"
    </automated>
  </verify>
  <done>
    /config redirects to /settings?tab=sources. /settings/reports redirects to /settings.
  </done>
</task>

<task type="auto">
  <name>Task 3: Update sidebar navigation links</name>
  <files>components/sidebar/sidebar.tsx</files>
  <read_first>
    - components/sidebar/sidebar.tsx (existing sidebar component)
  </read_first>
  <action>
    In components/sidebar/sidebar.tsx:

    Per D-04 from CONTEXT.md:
    - "报告设置" nav item should link to "/settings" (Daily tab active)
    - "Config" nav item should link to "/settings?tab=sources"

    Find the nav items with activeNav="settings/reports" and activeNav="config".
    Update their href/URL to:
    - activeNav="settings/reports" → href="/settings" (or "/settings?tab=daily")
    - activeNav="config" → href="/settings?tab=sources"

    Also update the activeNav value strings if they are used for active state detection:
    - If checking for active nav item, both should now point to "settings" with different tab params
    - Or keep the activeNav values as-is and update the href only

    Preserve all existing styling and behavior. Do not add or remove nav items.
  </action>
  <verify>
    <automated>
      grep -n "settings" components/sidebar/sidebar.tsx | grep -E "(href|to)=.*settings"
    </automated>
  </verify>
  <done>
    Sidebar nav items updated: 报告设置 → /settings, Config → /settings?tab=sources
  </done>
</task>

</tasks>

<verification>
After Plan 02:
- app/settings/page.tsx exists with Tabs component
- GET /settings returns 200 with tabbed interface
- GET /config returns 301 to /settings?tab=sources
- GET /settings/reports returns 301 to /settings
- Sidebar links updated per D-04
- Sources tab renders ConfigPage
- Daily/Weekly tabs render report settings sections
</verification>

<success_criteria>
- /settings page renders with 日报, 周报, 数据源 tabs
- Tab switching changes URL (?tab= param)
- /config redirects to /settings?tab=sources (301)
- /settings/reports redirects to /settings (301)
- Sidebar: 报告设置 links to /settings, Config links to /settings?tab=sources
- Sources tab shows ConfigPage component
- No console errors on tab switching
</success_criteria>

<output>
After completion, create `.planning/phases/01-settings-consolidation/01-02-SUMMARY.md`
</output>
