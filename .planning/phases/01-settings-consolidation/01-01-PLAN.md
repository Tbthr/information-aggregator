---
phase: 01-settings-consolidation
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - components/report-settings-page.tsx
autonomous: true
requirements:
  - FRONTEND-01

must_haves:
  truths:
    - "Frontend sends topicIds (not packs) in PUT body to /api/settings/reports"
    - "Frontend reads settings.daily.topicIds (not settings.daily.packs) from API response"
    - "Topic selection persists after page reload"
    - "Checkbox label shows '数据源 Topic' (not '数据源 Pack')"
  artifacts:
    - path: components/report-settings-page.tsx
      provides: Report settings form with corrected field names
      contains: "topicIds"
    - path: components/report-settings-page.tsx
      provides: DailyConfig interface with topicIds field
      contains: "topicIds: string[]"
  key_links:
    - from: components/report-settings-page.tsx
      to: /api/settings/reports
      via: PUT body with topicIds field
      pattern: "topicIds.*PUT| PUT.*topicIds"
    - from: /api/settings/reports (GET response)
      to: components/report-settings-page.tsx
      via: settings.daily.topicIds read in useEffect sync
      pattern: "settings\\.daily\\.topicIds"

---

<objective>
Fix the `packs` vs `topicIds` field name mismatch bug in ReportSettingsPage. The backend API already uses `topicIds` correctly; the frontend must be updated to match. This is a prerequisite for the tabbed settings page (Plan 02) which imports ReportSettingsPage.
</objective>

<context>
@components/report-settings-page.tsx
@app/api/settings/reports/route.ts
@hooks/use-api.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rename packs to topicIds throughout ReportSettingsPage</name>
  <files>components/report-settings-page.tsx</files>
  <read_first>
    - components/report-settings-page.tsx (current implementation with packs field)
    - app/api/settings/reports/route.ts (backend schema confirming topicIds is correct)
  </read_first>
  <action>
    In components/report-settings-page.tsx, perform the following atomic changes:

    1. In the DailyConfig interface (or equivalent type definition near the top of the file):
       - Change `packs: string[]` to `topicIds: string[]`

    2. In the state initializer (the initial state object passed to useState for daily config):
       - Change `packs: []` to `topicIds: []`

    3. In the sync effect that reads from settings.daily (typically a useEffect with settings.daily.packs dependency):
       - Change `settings.daily.packs` to `settings.daily.topicIds`
       - Change `daily.packs` to `daily.topicIds` in the setDaily call

    4. In the togglePack function (renamed to toggleTopicId):
       - Change the field name from `packs` to `topicIds`
       - Change the setDaily call to use `topicIds` instead of `packs`
       - Rename the function from `togglePack` to `toggleTopicId`

    5. In the handleSave PUT body:
       - Change `packs: daily.packs` to `topicIds: daily.topicIds`

    6. In the checkbox label text:
       - Change "数据源 Pack" to "数据源 Topic"

    7. In the checkbox onChange handler reference:
       - Change `togglePack` to `toggleTopicId`

    Do NOT change keywordBlacklist, kindPreferences, or filterPrompt fields (D-12, D-13, D-14).

    Note: If there are multiple similar patterns for weekly config (weekly.packs), apply the same changes to the weekly section as well.
  </action>
  <verify>
    <automated>
      # Verify no "packs" references remain in ReportSettingsPage (except deferred fields)
      grep -n "packs" components/report-settings-page.tsx | grep -v "keywordBlacklist\|filterPrompt\|kindPreferences" || echo "No packs references found - PASS"
    </automated>
  </verify>
  <verify>
    <behavioral>
      - pnpm check passes (TypeScript correct)
      - grep confirms topicIds: string[] in DailyConfig interface
      - grep confirms settings.daily.topicIds in sync effect
    </behavioral>
  </verify>
  <done>
    ReportSettingsPage sends topicIds in PUT body, reads settings.daily.topicIds, and displays "数据源 Topic" label. Topic selection persists after reload.
  </done>
</task>

</tasks>

<verification>
After Plan 01:
- components/report-settings-page.tsx contains no `packs` field references (except deferred fields)
- components/report-settings-page.tsx contains `topicIds: string[]` in DailyConfig interface
- API GET /api/settings/reports returns { daily: { topicIds: [...] }, weekly: { topicIds: [...] } }
- Frontend correctly displays saved topicIds on page load
</verification>

<success_criteria>
- DailyConfig interface has `topicIds: string[]` (not `packs`)
- State initializer uses `topicIds: []`
- Sync effect reads `settings.daily.topicIds`
- handleSave sends `topicIds: daily.topicIds` in PUT body
- toggleTopicId function toggles the topicIds array
- Checkbox label is "数据源 Topic"
- grep confirms no errant `packs` field references remain
</success_criteria>

<output>
After completion, create `.planning/phases/01-settings-consolidation/01-01-SUMMARY.md`
</output>
